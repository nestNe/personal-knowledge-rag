spring:
  application:
    name: agent-guard-starter
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: deepseek-r1:7b
        options:
          temperature: 0.7
          num-predict: 512 # 可选：最大生成 token 数